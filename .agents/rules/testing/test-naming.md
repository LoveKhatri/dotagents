# Test naming

Test names should describe the scenario and expected behavior: `it('should return empty array when input is null')`.

Avoid generic names like `it('works')` or `it('handles edge cases')`.
