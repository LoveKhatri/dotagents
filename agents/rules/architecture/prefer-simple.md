# Prefer simple solutions

Start with the simplest implementation that meets requirements. Don't introduce abstractions, patterns, or libraries until the current code proves they're necessary.

Three similar lines of code is better than a premature abstraction.
