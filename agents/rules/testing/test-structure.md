# Test file structure

Tests go alongside the code they test, not in a separate `__tests__` or `test/` directory (unless the tool forces it).

Use `*.test.ts` or `*.spec.ts` — pick one per project.

Each test file covers a single source file or a single feature. Don't create omnibus test files.
